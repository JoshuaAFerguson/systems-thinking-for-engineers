# Module 07 — Exercises

Work these after the readings. Keep answers here (or link out) so the trail is visible.

---

### Exercise 7.1 — Match the scenario to the archetype

For each scenario, name the archetype and the one line that gives it away.

| # | Scenario | Archetype | Tell |
|---|----------|-----------|------|
| a | A team adds retries to hide a flaky dependency; six months later the dependency is flakier and nobody knows why. | | |
| b | Signups keep climbing, then plateau hard; doubling the ad budget does nothing. | | |
| c | Every team dumps large artifacts on the shared build cache until it thrashes for everyone. | | |
| d | The well-staffed service ships features, which justifies more staffing, while the understaffed one falls further behind. | | |
| e | The latency SLO keeps getting relaxed a little after each bad quarter. | | |
| f | Two services each raise their timeout because the other raised theirs. | | |
| g | On-call keeps hand-restarting the service nightly; the auto-recovery work never gets prioritized. | | |

> _Your answers (fill the table above, or restate here):_

---

### Exercise 7.2 — Draw the loops

Pick **two or three** archetypes and draw each one as a causal loop diagram. Label every loop as
R or B, mark the **delay(s)**, and circle the variable where the "way out" intervenes.

> _Sketch here (ASCII, a committed image, or a link to LOOPY):_
>
> _Archetype 1:_
>
> _Archetype 2:_
>
> _Archetype 3 (optional):_

---

### Exercise 7.3 — Recall the escape

From memory, write the **way out** for each archetype in one line. The point is to recall the
*counterintuitive* escape, not the obvious "do more of the fix."

| Archetype | Way out (one line) |
|-----------|--------------------|
| Fixes that Fail | |
| Shifting the Burden | |
| Limits to Growth | |
| Tragedy of the Commons | |
| Escalation | |
| Success to the Successful | |
| Drift to Low Performance | |
| Policy Resistance | |
| Seeking the Wrong Goal | |

---

### Exercise 7.4 — The obvious push backfires

Take any one archetype and describe the intervention a well-meaning manager would reach for first
(the "push harder" move) — and trace, loop by loop, why it makes the trap *worse*.

> _Your answer:_
