# Module 02 — Exercises

Work these after the readings. A pencil and paper (or a whiteboard photo) is enough — keep
sketches here or link them so the trail is visible.

---

### Exercise 2.1 — Name the stock, name the flows

Pick a stock from your world (a backlog, tech debt, open incidents, headcount, cash runway,
unread email). Fill in:

| | |
|---|---|
| **Stock** (the accumulation) | |
| **Inflow(s)** (what raises it) | |
| **Outflow(s)** (what lowers it) | |
| **Unit of the stock** | |
| **Unit of the flows** | |

Sanity check: the flow units should be the stock's unit *per time* (e.g., stock = tickets,
flow = tickets/day).

> _Notes:_

---

### Exercise 2.2 — Model a bathtub

Pick one of these (or your own) and write it as `stock = inflow − outflow`:

- `headcount = hiring − attrition`
- `backlog = arrivals − completions`
- `incidents_open = new_incidents − resolutions`

Then answer: what happens to the stock when inflow > outflow? When inflow < outflow? When
they're equal? Describe each in one line.

> _My bathtub equation:_
>
> _Inflow > outflow:_
>
> _Inflow < outflow:_
>
> _Inflow = outflow:_

---

### Exercise 2.3 — Draw the behavior-over-time graphs

For your chosen stock, sketch three BOTGs (stock on the y-axis, time on the x-axis):

1. Inflow held constant *above* outflow.
2. Outflow suddenly doubled while inflow stays fixed.
3. Inflow and outflow equal, then inflow cut to zero.

Label the axes and the shape (linear rise, decay, plateau). Paste or link your sketch.

> _Sketch / link:_

---

### Exercise 2.4 — The direct-change trap

Describe a time someone tried to change a **stock directly** — "just clear the backlog," "get
headcount to 50" — without changing a flow. What happened, and which flow should they have
turned instead?

> _The stock they targeted:_
>
> _The flow they ignored:_
>
> _What happened:_
